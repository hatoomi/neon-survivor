2026-03-20
- Coinglass collector: BTC-only runbook established; outputs saved to data/btc_latest.json and data/btc_latest.txt on demand.
- Confirmed working v4 endpoints (HTTP 200 with data):
  - OI aggregated history (h1): /api/futures/open-interest/aggregated-history → used latest close in report
  - Funding OI-weight history (h1): /api/futures/funding-rate/oi-weight-history → used latest close
  - ETF flow history: /api/etf/bitcoin/flow-history → used latest flow_usd
  - Options max pain: /api/option/max-pain?symbol=BTC&exchange=Deribit → used latest max_pain_price
- Endpoints that returned 200 but empty on this tenant/window: futures coins-markets (filtered), global-long-short-account-ratio (pair/symbol variants), futures indicators RSI. Logged and skipped in text.
- Practical fallbacks used:
  - Price/24h/high/low/volume from Binance public 24hr ticker (BTCUSDT)
  - Global long/short account ratio from Binance public futures (1h, last 4 points)
- Report template fixed to populate values directly from sources above; full report pasted to chat and written to data/btc_latest.txt.
- Do NOT store secrets; API key is provided per-run and not persisted.
- User request: retire/delete previous prompt runbooks and replace with a single final version for future use. Awaiting final text + target path/filename to save; will delete older versions on receipt and confirm.
- Title for the final runbook: "BTC DATA AGENT". Pending content from user; suggested save path: /data/.openclaw/workspace/docs/BTC_DATA_AGENT.md.
- Saved final runbook: /data/.openclaw/workspace/docs/BTC_DATA_AGENT.md (v1.0).
- Ran BTC DATA AGENT and produced populated report (price, OI latest, funding latest, liquidations, F&G, ETF flow, max pain). Remaining fields (exact OI 24h Δ, L/S breakdown, ETF top-3, Deribit options info details, RSI calc) queued for next enhancement.
- Follow-up metrics now fetched live: OI last and 24h-ago close; Binance L/S (global/top accounts/top positions) with 4h trend; ETF top-3 breakdown (IBIT/GBTC/FBTC); on-chain balance list (total sum + Binance/Coinbase 24h deltas). Taker and options info aggregation still returning empty/null — keep retry + alternative params.
- Rule update: Never use “available on request” in reports; use concrete values or “UNAVAILABLE — reason”.
- Delivered full report in chat with concrete values (OI delta, funding, L/S, liquidations, ETF breakdown, on-chain) and saved to data/btc_latest.txt.
- Remaining UNAVAILABLE items and reasons recorded: RSI (Binance klines compute errored in this run), Pi/Rainbow (index endpoints timed out at 3s), Taker (empty dataset), Options totals (need aggregation across per-contract rows).
